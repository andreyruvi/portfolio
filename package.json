{
  "name": "portfolio",
  "private": true,
  "type": "module",
  "description": "Architectural Design Studio — portfolio. Content lives in data/*.json; the HTML pages are generated by scripts/build.mjs.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "images": "node scripts/optimize-images.mjs",
    "serve": "node scripts/build.mjs && python3 -m http.server 8000"
  }
}
